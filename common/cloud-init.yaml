#cloud-config
package_update: true
packages:
  - curl
  - iptables

runcmd:
  - curl -fsSL https://tailscale.com/install.sh | sh
  - tailscale up --authkey="${tailnet-key}" --hostname="${hostname}" --ssh=true --advertise-tags="tag:server" --accept-routes=false
